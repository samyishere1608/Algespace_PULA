export enum NotebookState {
    Initial,
    Addition,
    Subtraction,
    Multiplication,
    Division,
    FirstSolution,
    EquationSelection,
    GoodChoice,
    BadChoice,
    NeutralChoice
}

export enum InputError {
    Validation,
    Evaluation,
    OutOfRange
}

export enum RowHeight {
    Single,
    Double,
    Triple
}

export enum OptimalEquation {
    First,
    Second,
    Both
}
