export enum Operator {
    Plus,
    Minus
}

export enum NumberType {
    Number,
    Fraction
}

export enum RelationSymbol {
    Equal,
    Larger,
    Smaller
}

export enum CoefficientContext {
    Plain,
    Weight,
    Multiplication
}

export enum Parenthesis {
    LeftSmall,
    LeftLarge,
    RightSmall,
    RightLarge
}
