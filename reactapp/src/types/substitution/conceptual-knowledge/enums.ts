export enum SubstitutionItemType {
    IsolatedVariable,
    SecondVariable,
    Coin
}

export enum SubstitutionGamePhase {
    Introduction,
    IsolatedVariableSelection,
    IsolatedVariableSubstitution,
    SubstitutionSolution,
    SecondVariableSelection,
    CoinSubstitution,
    SystemSolution
}

export enum SubstitutionLocation {
    FirstEquation,
    SecondEquation
}
